// Copy this file to  config.js  and fill in your own values.
// config.js is git-ignored — it must never be committed.
window.NIRA_CONFIG = {
  // Production webhook URL of the "Ultimate Personal Assistant" workflow.
  // n8n shows it on the Webhook node once the workflow is activated.
  WEBHOOK_URL: "https://YOUR-INSTANCE.app.n8n.cloud/webhook/YOUR-WEBHOOK-ID"
};
